############
# Bed Fans #
############

[gcode_macro _BEDFANVARS]
variable_threshold: 100    # Bed target temp where bed fans become active
variable_slow: 0.20        # Fan speed while bed is heating
variable_fast: 0.60        # Fan speed once bed temp is reached
gcode:

[fan_generic BedFans]
pin: BED_FAN
kick_start_time: 0.5
off_below: 0.10

[gcode_macro BEDFANSSLOW]
gcode:
    {% set SLOW = printer["gcode_macro _BEDFANVARS"].slow|float %}
    SET_FAN_SPEED FAN=BedFans SPEED={SLOW}

[gcode_macro BEDFANSFAST]
gcode:
    {% set FAST = printer["gcode_macro _BEDFANVARS"].fast|float %}
    SET_FAN_SPEED FAN=BedFans SPEED={FAST}

[gcode_macro BEDFANSOFF]
gcode:
    SET_FAN_SPEED FAN=BedFans SPEED=0


[gcode_macro SET_HEATER_TEMPERATURE]
rename_existing: _SET_HEATER_TEMPERATURE
gcode:
    {% set HEATER = params.HEATER|default("None") %}
    {% set TARGET = params.TARGET|default(0)|int %}
    {% set THRESHOLD = printer["gcode_macro _BEDFANVARS"].threshold|int %}

    {% if HEATER|lower == "extruder" %}
        _SET_HEATER_TEMPERATURE HEATER=extruder TARGET={TARGET}
    {% elif HEATER|lower == "heater_bed" %}
        _SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET={TARGET}
    {% else %}
        _SET_HEATER_TEMPERATURE {% for p in params %}{'%s=%s ' % (p, params[p])}{% endfor %}
    {% endif %}

    {% if HEATER|lower == "heater_bed" %}
        {% if TARGET >= THRESHOLD %}
            BEDFANSSLOW
            UPDATE_DELAYED_GCODE ID=bedfanloop DURATION=1
        {% else %}
            BEDFANSOFF
            UPDATE_DELAYED_GCODE ID=bedfanloop DURATION=0
        {% endif %}
    {% endif %}

[gcode_macro M140]
rename_existing: M99140
gcode:
    {% set S = params.S|default(0)|float %}
    SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET={S}

[gcode_macro M190]
rename_existing: M99190
gcode:
    {% set S = params.S|default(0)|int %}
    {% set THRESHOLD = printer["gcode_macro _BEDFANVARS"].threshold|int %}

    {% if S >= THRESHOLD %}
        BEDFANSSLOW
    {% else %}
        BEDFANSOFF
    {% endif %}

    M99140 S{S}

    {% if S != 0 %}
        TEMPERATURE_WAIT SENSOR=heater_bed MINIMUM={S} MAXIMUM={S + 5}
    {% endif %}

    {% if S >= THRESHOLD %}
        BEDFANSFAST
    {% endif %}

[gcode_macro TURN_OFF_HEATERS]
rename_existing: _TURN_OFF_HEATERS
gcode:
    BEDFANSOFF
    _TURN_OFF_HEATERS

[delayed_gcode bedfanloop]
gcode:
    {% set THRESHOLD = printer["gcode_macro _BEDFANVARS"].threshold|int %}
    {% if printer.heater_bed.target >= THRESHOLD %}
        {% if printer.heater_bed.temperature|int >= (printer.heater_bed.target|int - 1) %}
            BEDFANSFAST
        {% else %}
            UPDATE_DELAYED_GCODE ID=bedfanloop DURATION=5
        {% endif %}
    {% endif %}
